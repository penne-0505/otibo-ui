# Quarantined legacy frontend-design skills

These two files are byte-preserved copies of the former `.agents` and `.claude`
skill entries. They are deliberately outside both active skill-loader paths and
are **non-operational historical references**. Do not load, follow, or restore
them as project guidance.

They remain here because the migration must not bypass the deletion hook. Their
eventual deletion is deferred to an owner-authorized cleanup after the
provenance record in
[_docs/qa/Workflow/docs-template-v1-migration/verification.md](_docs/qa/Workflow/docs-template-v1-migration/verification.md)
is reviewed.
